       <div class="back">
         <div class="nav">
           <ul>
             <li class="logo"> Porto <span>Svan</span> </li>
             <li class="items"> <a href="#">Home</a> </li>
             <li class="items"> <a href="#">Projects</a> </li>
             <li class="items"> <a href="#">Services</a> </li>
             <li class="items"> <a href="#">About</a> </li>
             <li class="items"> <a href="#">Contact <i class="fas fa-chevron-down"></i>
               <div class="menu">
                 <ul>
                   <li> <a href="#">Gmail</a> </li>
                   <li> <a href="#">Number</a> </li>
                   <li> <a href="#">Email</a> </li>
                 </ul>
               </div>
             </a>
            </li>
           </ul>
         </div>
       </div>


* {
  box-sizing: border-box;
  padding: 0;
  margin: 0;
}

body {
  font-family: sans-serif;
}

:root {
  --secColor: #151515;
  --thiColor: darkred;
}

.back {
  background: darkred;
  height: 100vh;
}

.nav {
  background: #151515;
  padding: 5px 50px;
}

.nav ul {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  align-items: center;
  list-style: none;
}

.nav ul li.items {
  margin: 10px 0;
  padding: 15px 10px;
  text-align: center;
  position: relative;
}

.nav ul li.items::after {
  content: "";
  position: absolute;
  bottom: -5px;
  left: 0;
  width: 0;
  height: 2px;
  transition: .5s;
}

.nav ul li.items:hover::after {
  width: 102%;
  background: #fff;
}

.nav ul li a {
  color: #fff;
  text-decoration: none;
  font-size: 18px;
  transition: .3s;
  text-align: center;
}

.nav ul li.logo {
  color: #fff;
  font-size: 20px;
  font-weight: 600;
  user-select: none;
  flex: 1;
}

.nav ul li.logo span {
  background: #fff;
  color: #000;
  padding: 10px 15px;
  text-transform: uppercase;
  border-radius: 3px;
  display: inline-block;
}

.menu {
  display: none;
  position: relative;
}

.nav ul li.items:hover .menu {
  display: block;
  position: absolute;
  top: 260%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: #151515;
  z-index: 1;
}

.nav ul li.items:hover .menu ul {
  display: block;
  margin: 5px;
}

.nav ul li.items:hover .menu ul li {
  border-bottom: 1px solid #fff;
  padding: 15px 45px;
}

.nav ul li.items:hover .menu ul li:last-child {
  border: none;
}

.nav ul li.items:hover .menu ul li a {
  color: #787878;
}

.nav ul li.items:hover .menu ul li a:hover {
  color: #cccccc;
}

.nav ul li.items i {
  font-size: 16px;
}

@media (max-width: 1000px) {
  .nav {
    padding: 5px 40px;
  }

  .nav ul li.items {
    width: 100%;
    transition: .5s;
    cursor: pointer;
  }

  .nav ul li.items::after {
    opacity: 0;
  }

  .nav ul li.items:hover {
    box-shadow: inset 0 0 10px #fff,
                inset 0 0 10px #fff;
  }

  .nav ul li a {
    font-size: 20px;
  }

  .nav ul li.logo {
    font-size: 24px;
  }
}
